#include "main.h"
#include <stdlib.h>

/**
 * _abs - Function to return the absolute value of an argument
 * @n: Integer to run the absolute function on
 * Return: absolute value of argument
 */

int _abs(int n)
{
	return (abs(n));
}
